::rc_st::__eval_from_socket_beginator
set lib_load_start [::legacy::get_attribute runtime /]
::legacy::set_attribute -quiet common_ui false /
::legacy::set_attribute -quiet rc_st::st_lib_script_loaded 1 /
rc_st::setup_log_dir
rc_st::setup_pbs_db_dir
if { ![llength [vfind / -library *]]} {
::legacy::set_attribute -quiet shm_id_rc 10584086 /
::legacy::set_attribute -quiet hostname_main twins2 /
::legacy::set_attribute -quiet pid_main 948 /
::legacy::set_attribute -quiet process_group_id 0 /
::legacy::set_attribute -quiet max_cpus_per_server 8 /
::legacy::set_attribute -quiet max_cpus_per_server_int 8 /
::legacy::set_attribute -quiet timing_use_ecsm_pin_capacitance true /
::legacy::set_attribute -quiet read_library_without_ecsm false /
::legacy::set_attribute -quiet read_ccs_noise_data false /
::legacy::set_attribute -quiet read_library_without_ccs false /
::legacy::set_attribute -quiet read_library_without_ecsm_timing true /
::legacy::set_attribute -quiet derive_bussed_pins false /
::legacy::set_attribute -quiet on_demand_library_loading false /
::legacy::set_attribute -quiet allow_template_optimisation true /
::legacy::set_attribute -quiet library_scaling 2 /
::legacy::set_attribute -quiet lbr_merge_lib_arcs 2 /
::legacy::set_attribute -quiet use_nextstate_type_only_to_assign_sync_ctrls false /
::legacy::set_attribute -quiet lbr_setup_constraint_sigma_multiplier 0.0 /
::legacy::set_attribute -quiet lbr_hold_constraint_sigma_multiplier 0.0 /
::legacy::set_attribute -quiet lbr_setup_constraint_sigma_type sigma /
::legacy::set_attribute -quiet lbr_hold_constraint_sigma_type sigma /
::legacy::set_attribute -quiet ignore_missing_library_file false /
::legacy::set_attribute -quiet lbr_enable_new_unit false /
::legacy::set_attribute -quiet init_lib_search_path {. /tools/cadence/GENUS171/tools.lnx86/lib/tech} /
}
set list_libraries [string trim [string map { \} {} \{ {} \" {} } [::legacy::get_attr library /]]]
if { "$list_libraries" ne "/pdk/st/cmos065_421/CORE65LPSVT_SNPS-AVT-CDS_4.1/libs/CORE65LPSVT_wc_1.25V_125C.lib  /pdk/st/cmos065_421/CLOCK65LPSVT_SNPS-AVT-CDS_2.1/libs/CLOCK65LPSVT_wc_1.25V_125C.lib"} {
::legacy::set_attribute library {/pdk/st/cmos065_421/CORE65LPSVT_SNPS-AVT-CDS_4.1/libs/CORE65LPSVT_wc_1.25V_125C.lib  /pdk/st/cmos065_421/CLOCK65LPSVT_SNPS-AVT-CDS_2.1/libs/CLOCK65LPSVT_wc_1.25V_125C.lib} /
}
set cur_attr_val [::legacy::get_attribute information_level /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 7] [list $cur_attr_val]]} {
if {[is_attr_ifca information_level ] && ![string compare 7 ""]} {
catch {::legacy::reset_attribute -quiet information_level / }
} else {
catch {::legacy::set_attribute -quiet information_level 7 /}
}
}
set cur_attr_val [::legacy::get_attribute within_regress /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca within_regress ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet within_regress / }
} else {
catch {::legacy::set_attribute -quiet within_regress false /}
}
}
set cur_attr_val [::legacy::get_attribute debug_qsort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca debug_qsort ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet debug_qsort / }
} else {
catch {::legacy::set_attribute -quiet debug_qsort false /}
}
}
set cur_attr_val [::legacy::get_attribute novus_ui /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca novus_ui ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet novus_ui / }
} else {
catch {::legacy::set_attribute -quiet novus_ui false /}
}
}
set cur_attr_val [::legacy::get_attribute common_ui /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca common_ui ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet common_ui / }
} else {
catch {::legacy::set_attribute -quiet common_ui false /}
}
}
set cur_attr_val [::legacy::get_attribute path /]
if {(17 || [llength [split $cur_attr_val]]) && [string compare [list {. / /libraries/* /libraries/library_domains/*/* /libraries/library_sets/*/* /libraries/library_domains/* /libraries/library_sets/* /designs/* /designs/*/timing/clocks/ /designs/*/timing/clock_domains/* /designs/*/dft/test_clock_domains/* /designs/*/dft/mbist/mbist_clock_domains/* /designs/*/modes /designs/*/modes/*/clocks /designs/*/modes/*/clock_domains/* /mmmc_designs_spec/* /designs/*/mmmc/*}] [list $cur_attr_val]]} {
if {[is_attr_ifca path ] && ![string compare {. / /libraries/* /libraries/library_domains/*/* /libraries/library_sets/*/* /libraries/library_domains/* /libraries/library_sets/* /designs/* /designs/*/timing/clocks/ /designs/*/timing/clock_domains/* /designs/*/dft/test_clock_domains/* /designs/*/dft/mbist/mbist_clock_domains/* /designs/*/modes /designs/*/modes/*/clocks /designs/*/modes/*/clock_domains/* /mmmc_designs_spec/* /designs/*/mmmc/*} ""]} {
catch {::legacy::reset_attribute -quiet path / }
} else {
catch {::legacy::set_attribute -quiet path {. / /libraries/* /libraries/library_domains/*/* /libraries/library_sets/*/* /libraries/library_domains/* /libraries/library_sets/* /designs/* /designs/*/timing/clocks/ /designs/*/timing/clock_domains/* /designs/*/dft/test_clock_domains/* /designs/*/dft/mbist/mbist_clock_domains/* /designs/*/modes /designs/*/modes/*/clocks /designs/*/modes/*/clock_domains/* /mmmc_designs_spec/* /designs/*/mmmc/*} /}
}
}
set cur_attr_val [::legacy::get_attribute hostname_main /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list twins2] [list $cur_attr_val]]} {
if {[is_attr_ifca hostname_main ] && ![string compare twins2 ""]} {
catch {::legacy::reset_attribute -quiet hostname_main / }
} else {
catch {::legacy::set_attribute -quiet hostname_main twins2 /}
}
}
set cur_attr_val [::legacy::get_attribute pid_main /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 948] [list $cur_attr_val]]} {
if {[is_attr_ifca pid_main ] && ![string compare 948 ""]} {
catch {::legacy::reset_attribute -quiet pid_main / }
} else {
catch {::legacy::set_attribute -quiet pid_main 948 /}
}
}
set cur_attr_val [::legacy::get_attribute operating_conditions_analysis_type /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca operating_conditions_analysis_type ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet operating_conditions_analysis_type / }
} else {
catch {::legacy::set_attribute -quiet operating_conditions_analysis_type {} /}
}
}
set cur_attr_val [::legacy::get_attribute operating_conditions_min /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca operating_conditions_min ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet operating_conditions_min / }
} else {
catch {::legacy::set_attribute -quiet operating_conditions_min {} /}
}
}
set cur_attr_val [::legacy::get_attribute operating_conditions_min_library /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca operating_conditions_min_library ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet operating_conditions_min_library / }
} else {
catch {::legacy::set_attribute -quiet operating_conditions_min_library {} /}
}
}
set cur_attr_val [::legacy::get_attribute operating_conditions_library_given /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca operating_conditions_library_given ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet operating_conditions_library_given / }
} else {
catch {::legacy::set_attribute -quiet operating_conditions_library_given false /}
}
}
set cur_attr_val [::legacy::get_attribute optimize_net_area /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca optimize_net_area ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet optimize_net_area / }
} else {
catch {::legacy::set_attribute -quiet optimize_net_area true /}
}
}
set cur_attr_val [::legacy::get_attribute net_area_weight_pct /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 100] [list $cur_attr_val]]} {
if {[is_attr_ifca net_area_weight_pct ] && ![string compare 100 ""]} {
catch {::legacy::reset_attribute -quiet net_area_weight_pct / }
} else {
catch {::legacy::set_attribute -quiet net_area_weight_pct 100 /}
}
}
set cur_attr_val [::legacy::get_attribute inst_prefix /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list g] [list $cur_attr_val]]} {
if {[is_attr_ifca inst_prefix ] && ![string compare g ""]} {
catch {::legacy::reset_attribute -quiet inst_prefix / }
} else {
catch {::legacy::set_attribute -quiet inst_prefix g /}
}
}
set cur_attr_val [::legacy::get_attribute ungroup_separator /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list _] [list $cur_attr_val]]} {
if {[is_attr_ifca ungroup_separator ] && ![string compare _ ""]} {
catch {::legacy::reset_attribute -quiet ungroup_separator / }
} else {
catch {::legacy::set_attribute -quiet ungroup_separator _ /}
}
}
set cur_attr_val [::legacy::get_attribute default_var_direction_is_downward /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca default_var_direction_is_downward ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet default_var_direction_is_downward / }
} else {
catch {::legacy::set_attribute -quiet default_var_direction_is_downward true /}
}
}
set cur_attr_val [::legacy::get_attribute tns_opto /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca tns_opto ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet tns_opto / }
} else {
catch {::legacy::set_attribute -quiet tns_opto true /}
}
}
set cur_attr_val [::legacy::get_attribute write_vlog_unconnected_port_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list full] [list $cur_attr_val]]} {
if {[is_attr_ifca write_vlog_unconnected_port_style ] && ![string compare full ""]} {
catch {::legacy::reset_attribute -quiet write_vlog_unconnected_port_style / }
} else {
catch {::legacy::set_attribute -quiet write_vlog_unconnected_port_style full /}
}
}
set cur_attr_val [::legacy::get_attribute compress_exceptions_during_synthesis /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca compress_exceptions_during_synthesis ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet compress_exceptions_during_synthesis / }
} else {
catch {::legacy::set_attribute -quiet compress_exceptions_during_synthesis true /}
}
}
set cur_attr_val [::legacy::get_attribute probabilistic_extraction /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca probabilistic_extraction ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet probabilistic_extraction / }
} else {
catch {::legacy::set_attribute -quiet probabilistic_extraction true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_use_segment_parasitics /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_use_segment_parasitics ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_use_segment_parasitics / }
} else {
catch {::legacy::set_attribute -quiet phys_use_segment_parasitics true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_use_segment_resistance /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_use_segment_resistance ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_use_segment_resistance / }
} else {
catch {::legacy::set_attribute -quiet phys_use_segment_resistance true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_hum_route /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_hum_route ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_hum_route / }
} else {
catch {::legacy::set_attribute -quiet phys_hum_route true /}
}
}
set cur_attr_val [::legacy::get_attribute design_mode_process /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 100.0] [list $cur_attr_val]]} {
if {[is_attr_ifca design_mode_process ] && ![string compare 100.0 ""]} {
catch {::legacy::reset_attribute -quiet design_mode_process / }
} else {
catch {::legacy::set_attribute -quiet design_mode_process 100.0 /}
}
}
set cur_attr_val [::legacy::get_attribute fence_as_blockage_buffer_tree /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca fence_as_blockage_buffer_tree ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet fence_as_blockage_buffer_tree / }
} else {
catch {::legacy::set_attribute -quiet fence_as_blockage_buffer_tree false /}
}
}
set cur_attr_val [::legacy::get_attribute report_layer_bin /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca report_layer_bin ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet report_layer_bin / }
} else {
catch {::legacy::set_attribute -quiet report_layer_bin false /}
}
}
set cur_attr_val [::legacy::get_attribute phys_assume_met_fill /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_assume_met_fill ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet phys_assume_met_fill / }
} else {
catch {::legacy::set_attribute -quiet phys_assume_met_fill 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute map_placed_for_hum /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca map_placed_for_hum ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet map_placed_for_hum / }
} else {
catch {::legacy::set_attribute -quiet map_placed_for_hum false /}
}
}
set cur_attr_val [::legacy::get_attribute phys_use_invs_extraction /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_use_invs_extraction ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_use_invs_extraction / }
} else {
catch {::legacy::set_attribute -quiet phys_use_invs_extraction true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_route_time_out /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 120.0] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_route_time_out ] && ![string compare 120.0 ""]} {
catch {::legacy::reset_attribute -quiet phys_route_time_out / }
} else {
catch {::legacy::set_attribute -quiet phys_route_time_out 120.0 /}
}
}
set cur_attr_val [::legacy::get_attribute phys_extra_grid_multiplier /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_extra_grid_multiplier ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet phys_extra_grid_multiplier / }
} else {
catch {::legacy::set_attribute -quiet phys_extra_grid_multiplier 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute phys_use_extraction_kit /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_use_extraction_kit ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet phys_use_extraction_kit / }
} else {
catch {::legacy::set_attribute -quiet phys_use_extraction_kit false /}
}
}
set cur_attr_val [::legacy::get_attribute phys_vias_for_routing /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_vias_for_routing ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet phys_vias_for_routing / }
} else {
catch {::legacy::set_attribute -quiet phys_vias_for_routing {} /}
}
}
set cur_attr_val [::legacy::get_attribute _scale_of_cap_per_unit_length_h /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca _scale_of_cap_per_unit_length_h ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet _scale_of_cap_per_unit_length_h / }
} else {
catch {::legacy::set_attribute -quiet _scale_of_cap_per_unit_length_h 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute _scale_of_cap_per_unit_length_v /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca _scale_of_cap_per_unit_length_v ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet _scale_of_cap_per_unit_length_v / }
} else {
catch {::legacy::set_attribute -quiet _scale_of_cap_per_unit_length_v 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute _scale_of_res_per_unit_length_h /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca _scale_of_res_per_unit_length_h ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet _scale_of_res_per_unit_length_h / }
} else {
catch {::legacy::set_attribute -quiet _scale_of_res_per_unit_length_h 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute _scale_of_res_per_unit_length_v /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca _scale_of_res_per_unit_length_v ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet _scale_of_res_per_unit_length_v / }
} else {
catch {::legacy::set_attribute -quiet _scale_of_res_per_unit_length_v 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute extract_rc_lef_tech_file_map /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca extract_rc_lef_tech_file_map ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet extract_rc_lef_tech_file_map / }
} else {
catch {::legacy::set_attribute -quiet extract_rc_lef_tech_file_map {} /}
}
}
set cur_attr_val [::legacy::get_attribute capacitance_per_unit_length_mmmc /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca capacitance_per_unit_length_mmmc ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet capacitance_per_unit_length_mmmc / }
} else {
catch {::legacy::set_attribute -quiet capacitance_per_unit_length_mmmc {} /}
}
}
set cur_attr_val [::legacy::get_attribute resistance_per_unit_length_mmmc /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca resistance_per_unit_length_mmmc ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet resistance_per_unit_length_mmmc / }
} else {
catch {::legacy::set_attribute -quiet resistance_per_unit_length_mmmc {} /}
}
}
set cur_attr_val [::legacy::get_attribute global_route_overflow_TR_H /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca global_route_overflow_TR_H ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet global_route_overflow_TR_H / }
} else {
catch {::legacy::set_attribute -quiet global_route_overflow_TR_H 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute global_route_overflow_TR_V /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca global_route_overflow_TR_V ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet global_route_overflow_TR_V / }
} else {
catch {::legacy::set_attribute -quiet global_route_overflow_TR_V 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute lp_insert_clock_gating /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca lp_insert_clock_gating ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet lp_insert_clock_gating / }
} else {
catch {::legacy::set_attribute -quiet lp_insert_clock_gating false /}
}
}
set cur_attr_val [::legacy::get_attribute lp_power_unit /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list uW] [list $cur_attr_val]]} {
if {[is_attr_ifca lp_power_unit ] && ![string compare uW ""]} {
catch {::legacy::reset_attribute -quiet lp_power_unit / }
} else {
catch {::legacy::set_attribute -quiet lp_power_unit uW /}
}
}
set cur_attr_val [::legacy::get_attribute enable_runtime_by_stage /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca enable_runtime_by_stage ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet enable_runtime_by_stage / }
} else {
catch {::legacy::set_attribute -quiet enable_runtime_by_stage true /}
}
}
set cur_attr_val [::legacy::get_attribute case_handle_real_constants_too /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca case_handle_real_constants_too ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet case_handle_real_constants_too / }
} else {
catch {::legacy::set_attribute -quiet case_handle_real_constants_too true /}
}
}
set cur_attr_val [::legacy::get_attribute pbs_mmmc_adjust_fast /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs_mmmc_adjust_fast ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet pbs_mmmc_adjust_fast / }
} else {
catch {::legacy::set_attribute -quiet pbs_mmmc_adjust_fast true /}
}
}
set cur_attr_val [::legacy::get_attribute timing_use_ecsm_pin_capacitance /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca timing_use_ecsm_pin_capacitance ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet timing_use_ecsm_pin_capacitance / }
} else {
catch {::legacy::set_attribute -quiet timing_use_ecsm_pin_capacitance true /}
}
}
set cur_attr_val [::legacy::get_attribute write_pd_libcell_derate /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca write_pd_libcell_derate ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet write_pd_libcell_derate / }
} else {
catch {::legacy::set_attribute -quiet write_pd_libcell_derate false /}
}
}
set cur_attr_val [::legacy::get_attribute lp_clock_gating_auto_control /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list none] [list $cur_attr_val]]} {
if {[is_attr_ifca lp_clock_gating_auto_control ] && ![string compare none ""]} {
catch {::legacy::reset_attribute -quiet lp_clock_gating_auto_control / }
} else {
catch {::legacy::set_attribute -quiet lp_clock_gating_auto_control none /}
}
}
set cur_attr_val [::legacy::get_attribute send_row_col /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca send_row_col ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet send_row_col / }
} else {
catch {::legacy::set_attribute -quiet send_row_col false /}
}
}
set cur_attr_val [::legacy::get_attribute dft_shift_register_with_mbci /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca dft_shift_register_with_mbci ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet dft_shift_register_with_mbci / }
} else {
catch {::legacy::set_attribute -quiet dft_shift_register_with_mbci false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_large_fanout_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_large_fanout_threshold ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet iopt_large_fanout_threshold / }
} else {
catch {::legacy::set_attribute -quiet iopt_large_fanout_threshold no_value /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_idealize_large_fanout /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_idealize_large_fanout ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_idealize_large_fanout / }
} else {
catch {::legacy::set_attribute -quiet iopt_idealize_large_fanout false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_sequential_duplication_for_tns_opto /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_sequential_duplication_for_tns_opto ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_sequential_duplication_for_tns_opto / }
} else {
catch {::legacy::set_attribute -quiet iopt_sequential_duplication_for_tns_opto false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_relax_scan_checks_in_sequential_resynthesis /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_relax_scan_checks_in_sequential_resynthesis ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet iopt_relax_scan_checks_in_sequential_resynthesis / }
} else {
catch {::legacy::set_attribute -quiet iopt_relax_scan_checks_in_sequential_resynthesis 1 /}
}
}
set cur_attr_val [::legacy::get_attribute max_multiple_output_gate_support /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 3] [list $cur_attr_val]]} {
if {[is_attr_ifca max_multiple_output_gate_support ] && ![string compare 3 ""]} {
catch {::legacy::reset_attribute -quiet max_multiple_output_gate_support / }
} else {
catch {::legacy::set_attribute -quiet max_multiple_output_gate_support 3 /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_force_common_input_removal /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_force_common_input_removal ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_force_common_input_removal / }
} else {
catch {::legacy::set_attribute -quiet iopt_force_common_input_removal false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_ultra_optimization /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_ultra_optimization ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_ultra_optimization / }
} else {
catch {::legacy::set_attribute -quiet iopt_ultra_optimization false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_on_the_fly_configurations /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_on_the_fly_configurations ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet iopt_on_the_fly_configurations / }
} else {
catch {::legacy::set_attribute -quiet iopt_on_the_fly_configurations true /}
}
}
set cur_attr_val [::legacy::get_attribute global_incremental_ignores_power /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca global_incremental_ignores_power ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet global_incremental_ignores_power / }
} else {
catch {::legacy::set_attribute -quiet global_incremental_ignores_power false /}
}
}
set cur_attr_val [::legacy::get_attribute global_incremental_extra_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca global_incremental_extra_effort ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet global_incremental_extra_effort / }
} else {
catch {::legacy::set_attribute -quiet global_incremental_extra_effort false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_super_thread_debug /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_super_thread_debug ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet iopt_super_thread_debug / }
} else {
catch {::legacy::set_attribute -quiet iopt_super_thread_debug 0 /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_cell_count_optimization /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_cell_count_optimization ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_cell_count_optimization / }
} else {
catch {::legacy::set_attribute -quiet iopt_cell_count_optimization false /}
}
}
set cur_attr_val [::legacy::get_attribute multibit_stalone_logical /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca multibit_stalone_logical ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet multibit_stalone_logical / }
} else {
catch {::legacy::set_attribute -quiet multibit_stalone_logical false /}
}
}
set cur_attr_val [::legacy::get_attribute multibit_stalone_include_dyn_pow_net_area /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca multibit_stalone_include_dyn_pow_net_area ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet multibit_stalone_include_dyn_pow_net_area / }
} else {
catch {::legacy::set_attribute -quiet multibit_stalone_include_dyn_pow_net_area false /}
}
}
set cur_attr_val [::legacy::get_attribute dump_cluster_info_for_group /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list -1] [list $cur_attr_val]]} {
if {[is_attr_ifca dump_cluster_info_for_group ] && ![string compare -1 ""]} {
catch {::legacy::reset_attribute -quiet dump_cluster_info_for_group / }
} else {
catch {::legacy::set_attribute -quiet dump_cluster_info_for_group -1 /}
}
}
set cur_attr_val [::legacy::get_attribute multibit_ignore_exact_match_seq_sync_ctrls /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca multibit_ignore_exact_match_seq_sync_ctrls ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet multibit_ignore_exact_match_seq_sync_ctrls / }
} else {
catch {::legacy::set_attribute -quiet multibit_ignore_exact_match_seq_sync_ctrls false /}
}
}
set cur_attr_val [::legacy::get_attribute multibit_ignore_exact_match_seq_async_ctrls /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca multibit_ignore_exact_match_seq_async_ctrls ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet multibit_ignore_exact_match_seq_async_ctrls / }
} else {
catch {::legacy::set_attribute -quiet multibit_ignore_exact_match_seq_async_ctrls false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_enable_parallelization /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_enable_parallelization ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_enable_parallelization / }
} else {
catch {::legacy::set_attribute -quiet iopt_enable_parallelization false /}
}
}
set cur_attr_val [::legacy::get_attribute gps_max_partition_size /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 130000] [list $cur_attr_val]]} {
if {[is_attr_ifca gps_max_partition_size ] && ![string compare 130000 ""]} {
catch {::legacy::reset_attribute -quiet gps_max_partition_size / }
} else {
catch {::legacy::set_attribute -quiet gps_max_partition_size 130000 /}
}
}
set cur_attr_val [::legacy::get_attribute pbs_comet_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 100000] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs_comet_threshold ] && ![string compare 100000 ""]} {
catch {::legacy::reset_attribute -quiet pbs_comet_threshold / }
} else {
catch {::legacy::set_attribute -quiet pbs_comet_threshold 100000 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_vhdl_read_version /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1993] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_vhdl_read_version ] && ![string compare 1993 ""]} {
catch {::legacy::reset_attribute -quiet hdl_vhdl_read_version / }
} else {
catch {::legacy::set_attribute -quiet hdl_vhdl_read_version 1993 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_vhdl_environment /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list common] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_vhdl_environment ] && ![string compare common ""]} {
catch {::legacy::reset_attribute -quiet hdl_vhdl_environment / }
} else {
catch {::legacy::set_attribute -quiet hdl_vhdl_environment common /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_vhdl_case /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list original] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_vhdl_case ] && ![string compare original ""]} {
catch {::legacy::reset_attribute -quiet hdl_vhdl_case / }
} else {
catch {::legacy::set_attribute -quiet hdl_vhdl_case original /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_preserve_unused_registers /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_preserve_unused_registers ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_preserve_unused_registers / }
} else {
catch {::legacy::set_attribute -quiet hdl_preserve_unused_registers false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_preserve_unused_flop /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_preserve_unused_flop ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_preserve_unused_flop / }
} else {
catch {::legacy::set_attribute -quiet hdl_preserve_unused_flop false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_preserve_unused_latch /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_preserve_unused_latch ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_preserve_unused_latch / }
} else {
catch {::legacy::set_attribute -quiet hdl_preserve_unused_latch false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_error_on_latch /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_error_on_latch ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_error_on_latch / }
} else {
catch {::legacy::set_attribute -quiet hdl_error_on_latch true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_error_on_logic_abstract /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_error_on_logic_abstract ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_error_on_logic_abstract / }
} else {
catch {::legacy::set_attribute -quiet hdl_error_on_logic_abstract false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_parameter_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list _%s%d] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_parameter_naming_style ] && ![string compare _%s%d ""]} {
catch {::legacy::reset_attribute -quiet hdl_parameter_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_parameter_naming_style _%s%d /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_generate_index_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list {%s[%d]}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_generate_index_style ] && ![string compare {%s[%d]} ""]} {
catch {::legacy::reset_attribute -quiet hdl_generate_index_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_generate_index_style {%s[%d]} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_instance_array_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list {%s[%d]}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_instance_array_naming_style ] && ![string compare {%s[%d]} ""]} {
catch {::legacy::reset_attribute -quiet hdl_instance_array_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_instance_array_naming_style {%s[%d]} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_infer_ram /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_infer_ram ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_infer_ram / }
} else {
catch {::legacy::set_attribute -quiet hdl_infer_ram false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_infer_array_md /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_infer_array_md ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_infer_array_md / }
} else {
catch {::legacy::set_attribute -quiet hdl_infer_array_md false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_case_sensitive_instances /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list none] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_case_sensitive_instances ] && ![string compare none ""]} {
catch {::legacy::reset_attribute -quiet hdl_case_sensitive_instances / }
} else {
catch {::legacy::set_attribute -quiet hdl_case_sensitive_instances none /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_ff_keep_feedback /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_ff_keep_feedback ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_ff_keep_feedback / }
} else {
catch {::legacy::set_attribute -quiet hdl_ff_keep_feedback false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_ff_keep_explicit_feedback /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_ff_keep_explicit_feedback ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_ff_keep_explicit_feedback / }
} else {
catch {::legacy::set_attribute -quiet hdl_ff_keep_explicit_feedback true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_latch_keep_feedback /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_latch_keep_feedback ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_latch_keep_feedback / }
} else {
catch {::legacy::set_attribute -quiet hdl_latch_keep_feedback false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_common_subexpression_elimination /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_common_subexpression_elimination ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_common_subexpression_elimination / }
} else {
catch {::legacy::set_attribute -quiet hdl_common_subexpression_elimination true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_remove_cdfg_redundancies /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_remove_cdfg_redundancies ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_remove_cdfg_redundancies / }
} else {
catch {::legacy::set_attribute -quiet hdl_remove_cdfg_redundancies true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_map_ram /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list et_prim] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_map_ram ] && ![string compare et_prim ""]} {
catch {::legacy::reset_attribute -quiet hdl_map_ram / }
} else {
catch {::legacy::set_attribute -quiet hdl_map_ram et_prim /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_enable_slp /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_enable_slp ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_enable_slp / }
} else {
catch {::legacy::set_attribute -quiet hdl_enable_slp true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_extract_sum_of_products_logic /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_extract_sum_of_products_logic ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_extract_sum_of_products_logic / }
} else {
catch {::legacy::set_attribute -quiet hdl_extract_sum_of_products_logic true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_reverse_priority_mux_chain /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_reverse_priority_mux_chain ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_reverse_priority_mux_chain / }
} else {
catch {::legacy::set_attribute -quiet hdl_reverse_priority_mux_chain true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_combine_sop_nodes /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_combine_sop_nodes ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_combine_sop_nodes / }
} else {
catch {::legacy::set_attribute -quiet hdl_combine_sop_nodes 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_reduce_mux_chain /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_reduce_mux_chain ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_reduce_mux_chain / }
} else {
catch {::legacy::set_attribute -quiet hdl_reduce_mux_chain 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_infer_macro /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_infer_macro ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_infer_macro / }
} else {
catch {::legacy::set_attribute -quiet hdl_infer_macro 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_enable_bitwidth_analysis_based_pruning /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_enable_bitwidth_analysis_based_pruning ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_enable_bitwidth_analysis_based_pruning / }
} else {
catch {::legacy::set_attribute -quiet hdl_enable_bitwidth_analysis_based_pruning 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_enable_xform_constant_shiftrotates /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_enable_xform_constant_shiftrotates ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_enable_xform_constant_shiftrotates / }
} else {
catch {::legacy::set_attribute -quiet hdl_enable_xform_constant_shiftrotates 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_simplify_variable_part_selects /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_simplify_variable_part_selects ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet hdl_simplify_variable_part_selects / }
} else {
catch {::legacy::set_attribute -quiet hdl_simplify_variable_part_selects 1 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_strict_ieee_compliance /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_strict_ieee_compliance ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_strict_ieee_compliance / }
} else {
catch {::legacy::set_attribute -quiet hdl_strict_ieee_compliance false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_unconnected_value /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_unconnected_value ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet hdl_unconnected_value / }
} else {
catch {::legacy::set_attribute -quiet hdl_unconnected_value 0 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_unconnected_input_port_value /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_unconnected_input_port_value ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet hdl_unconnected_input_port_value / }
} else {
catch {::legacy::set_attribute -quiet hdl_unconnected_input_port_value 0 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_undriven_output_port_value /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_undriven_output_port_value ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet hdl_undriven_output_port_value / }
} else {
catch {::legacy::set_attribute -quiet hdl_undriven_output_port_value 0 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_undriven_signal_value /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_undriven_signal_value ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet hdl_undriven_signal_value / }
} else {
catch {::legacy::set_attribute -quiet hdl_undriven_signal_value 0 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_error_on_blackbox /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_error_on_blackbox ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_error_on_blackbox / }
} else {
catch {::legacy::set_attribute -quiet hdl_error_on_blackbox true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_rename_cdn_flop_pins /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_rename_cdn_flop_pins ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet hdl_rename_cdn_flop_pins / }
} else {
catch {::legacy::set_attribute -quiet hdl_rename_cdn_flop_pins {} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_reg_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list %s_reg%s] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_reg_naming_style ] && ![string compare %s_reg%s ""]} {
catch {::legacy::reset_attribute -quiet hdl_reg_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_reg_naming_style %s_reg%s /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_error_on_negedge /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_error_on_negedge ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_error_on_negedge / }
} else {
catch {::legacy::set_attribute -quiet hdl_error_on_negedge true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_new_case_logic /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_new_case_logic ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_new_case_logic / }
} else {
catch {::legacy::set_attribute -quiet hdl_new_case_logic true /}
}
}
set cur_attr_val [::legacy::get_attribute use_abstract_models /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca use_abstract_models ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet use_abstract_models / }
} else {
catch {::legacy::set_attribute -quiet use_abstract_models false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_resolve_parameterized_instance_with_structural_module /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_resolve_parameterized_instance_with_structural_module ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_resolve_parameterized_instance_with_structural_module / }
} else {
catch {::legacy::set_attribute -quiet hdl_resolve_parameterized_instance_with_structural_module false /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_use_parameterized_module_by_name /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_use_parameterized_module_by_name ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet hdl_use_parameterized_module_by_name / }
} else {
catch {::legacy::set_attribute -quiet hdl_use_parameterized_module_by_name false /}
}
}
set cur_attr_val [::legacy::get_attribute input_pragma_keyword /]
if {(5 || [llength [split $cur_attr_val]]) && [string compare [list {cadence synopsys ambit pragma synthesis}] [list $cur_attr_val]]} {
if {[is_attr_ifca input_pragma_keyword ] && ![string compare {cadence synopsys ambit pragma synthesis} ""]} {
catch {::legacy::reset_attribute -quiet input_pragma_keyword / }
} else {
catch {::legacy::set_attribute -quiet input_pragma_keyword {cadence synopsys ambit pragma synthesis} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_ignore_pragma_names /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list coverage] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_ignore_pragma_names ] && ![string compare coverage ""]} {
catch {::legacy::reset_attribute -quiet hdl_ignore_pragma_names / }
} else {
catch {::legacy::set_attribute -quiet hdl_ignore_pragma_names coverage /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_language /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list v2001] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_language ] && ![string compare v2001 ""]} {
catch {::legacy::reset_attribute -quiet hdl_language / }
} else {
catch {::legacy::set_attribute -quiet hdl_language v2001 /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_sync_set_reset /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_sync_set_reset ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet hdl_sync_set_reset / }
} else {
catch {::legacy::set_attribute -quiet hdl_sync_set_reset {} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_async_set_reset /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_async_set_reset ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet hdl_async_set_reset / }
} else {
catch {::legacy::set_attribute -quiet hdl_async_set_reset {} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_retain_subprogram_hier /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_retain_subprogram_hier ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet hdl_retain_subprogram_hier / }
} else {
catch {::legacy::set_attribute -quiet hdl_retain_subprogram_hier {} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_bus_wire_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list {%s[%d]}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_bus_wire_naming_style ] && ![string compare {%s[%d]} ""]} {
catch {::legacy::reset_attribute -quiet hdl_bus_wire_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_bus_wire_naming_style {%s[%d]} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_array_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list {%s[%d]}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_array_naming_style ] && ![string compare {%s[%d]} ""]} {
catch {::legacy::reset_attribute -quiet hdl_array_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_array_naming_style {%s[%d]} /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_record_naming_style /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list {%s[%s]}] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_record_naming_style ] && ![string compare {%s[%s]} ""]} {
catch {::legacy::reset_attribute -quiet hdl_record_naming_style / }
} else {
catch {::legacy::set_attribute -quiet hdl_record_naming_style {%s[%s]} /}
}
}
set cur_attr_val [::legacy::get_attribute flatten_array /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list flatten_array] [list $cur_attr_val]]} {
if {[is_attr_ifca flatten_array ] && ![string compare flatten_array ""]} {
catch {::legacy::reset_attribute -quiet flatten_array / }
} else {
catch {::legacy::set_attribute -quiet flatten_array flatten_array /}
}
}
set cur_attr_val [::legacy::get_attribute inline /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list inline] [list $cur_attr_val]]} {
if {[is_attr_ifca inline ] && ![string compare inline ""]} {
catch {::legacy::reset_attribute -quiet inline / }
} else {
catch {::legacy::set_attribute -quiet inline inline /}
}
}
set cur_attr_val [::legacy::get_attribute proto_hdl /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca proto_hdl ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet proto_hdl / }
} else {
catch {::legacy::set_attribute -quiet proto_hdl false /}
}
}
set cur_attr_val [::legacy::get_attribute load_tcl_modules /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list rc] [list $cur_attr_val]]} {
if {[is_attr_ifca load_tcl_modules ] && ![string compare rc ""]} {
catch {::legacy::reset_attribute -quiet load_tcl_modules / }
} else {
catch {::legacy::set_attribute -quiet load_tcl_modules rc /}
}
}
set cur_attr_val [::legacy::get_attribute program_debug_info /]
if {(8 || [llength [split $cur_attr_val]]) && [string compare [list {17.10-p007_1 17.1.7 {Aug  3 2017} /tools/cadence/GENUS171/tools.lnx86/synth/bin/64bit/genus /tools/cadence/GENUS171/tools.lnx86}] [list $cur_attr_val]]} {
if {[is_attr_ifca program_debug_info ] && ![string compare {17.10-p007_1 17.1.7 {Aug  3 2017} /tools/cadence/GENUS171/tools.lnx86/synth/bin/64bit/genus /tools/cadence/GENUS171/tools.lnx86} ""]} {
catch {::legacy::reset_attribute -quiet program_debug_info / }
} else {
catch {::legacy::set_attribute -quiet program_debug_info {17.10-p007_1 17.1.7 {Aug  3 2017} /tools/cadence/GENUS171/tools.lnx86/synth/bin/64bit/genus /tools/cadence/GENUS171/tools.lnx86} /}
}
}
set cur_attr_val [::legacy::get_attribute cle__disable_extra_space /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca cle__disable_extra_space ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet cle__disable_extra_space / }
} else {
catch {::legacy::set_attribute -quiet cle__disable_extra_space 0 /}
}
}
set cur_attr_val [::legacy::get_attribute legacy_before_eval_novus /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca legacy_before_eval_novus ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet legacy_before_eval_novus / }
} else {
catch {::legacy::set_attribute -quiet legacy_before_eval_novus false /}
}
}
set cur_attr_val [::legacy::get_attribute novus_before_eval_legacy /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca novus_before_eval_legacy ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet novus_before_eval_legacy / }
} else {
catch {::legacy::set_attribute -quiet novus_before_eval_legacy false /}
}
}
set cur_attr_val [::legacy::get_attribute dft_native_boundary_scan /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca dft_native_boundary_scan ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet dft_native_boundary_scan / }
} else {
catch {::legacy::set_attribute -quiet dft_native_boundary_scan true /}
}
}
set cur_attr_val [::legacy::get_attribute dft__within_replace_scan /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca dft__within_replace_scan ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet dft__within_replace_scan / }
} else {
catch {::legacy::set_attribute -quiet dft__within_replace_scan 0 /}
}
}
set cur_attr_val [::legacy::get_attribute dft__within_replace_scan_preprocessing /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca dft__within_replace_scan_preprocessing ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet dft__within_replace_scan_preprocessing / }
} else {
catch {::legacy::set_attribute -quiet dft__within_replace_scan_preprocessing 0 /}
}
}
set cur_attr_val [::legacy::get_attribute eta__debug /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca eta__debug ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet eta__debug / }
} else {
catch {::legacy::set_attribute -quiet eta__debug 0 /}
}
}
set cur_attr_val [::legacy::get_attribute suppress_messages /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca suppress_messages ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet suppress_messages / }
} else {
catch {::legacy::set_attribute -quiet suppress_messages false /}
}
}
set cur_attr_val [::legacy::get_attribute degenerate_combo_elts_to_basic /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca degenerate_combo_elts_to_basic ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet degenerate_combo_elts_to_basic / }
} else {
catch {::legacy::set_attribute -quiet degenerate_combo_elts_to_basic 0 /}
}
}
set cur_attr_val [::legacy::get_attribute spef__print_swapping_stats /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca spef__print_swapping_stats ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet spef__print_swapping_stats / }
} else {
catch {::legacy::set_attribute -quiet spef__print_swapping_stats 0 /}
}
}
set cur_attr_val [::legacy::get_attribute spef__min_cap_ratio_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca spef__min_cap_ratio_threshold ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet spef__min_cap_ratio_threshold / }
} else {
catch {::legacy::set_attribute -quiet spef__min_cap_ratio_threshold 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute spef__detour_net_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca spef__detour_net_threshold ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet spef__detour_net_threshold / }
} else {
catch {::legacy::set_attribute -quiet spef__detour_net_threshold 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute spef__site_size_multiple_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0.0] [list $cur_attr_val]]} {
if {[is_attr_ifca spef__site_size_multiple_threshold ] && ![string compare 0.0 ""]} {
catch {::legacy::reset_attribute -quiet spef__site_size_multiple_threshold / }
} else {
catch {::legacy::set_attribute -quiet spef__site_size_multiple_threshold 0.0 /}
}
}
set cur_attr_val [::legacy::get_attribute write_do_lec_use_fv_map /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca write_do_lec_use_fv_map ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet write_do_lec_use_fv_map / }
} else {
catch {::legacy::set_attribute -quiet write_do_lec_use_fv_map true /}
}
}
set cur_attr_val [::legacy::get_attribute write_vlog_insert_new_line_after_separator /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca write_vlog_insert_new_line_after_separator ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet write_vlog_insert_new_line_after_separator / }
} else {
catch {::legacy::set_attribute -quiet write_vlog_insert_new_line_after_separator 0 /}
}
}
set cur_attr_val [::legacy::get_attribute aupt__debug /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca aupt__debug ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet aupt__debug / }
} else {
catch {::legacy::set_attribute -quiet aupt__debug 0 /}
}
}
set cur_attr_val [::legacy::get_attribute aupt_module_size_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 80000] [list $cur_attr_val]]} {
if {[is_attr_ifca aupt_module_size_threshold ] && ![string compare 80000 ""]} {
catch {::legacy::reset_attribute -quiet aupt_module_size_threshold / }
} else {
catch {::legacy::set_attribute -quiet aupt_module_size_threshold 80000 /}
}
}
set cur_attr_val [::legacy::get_attribute aupt_combo_input_count_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 150000] [list $cur_attr_val]]} {
if {[is_attr_ifca aupt_combo_input_count_threshold ] && ![string compare 150000 ""]} {
catch {::legacy::reset_attribute -quiet aupt_combo_input_count_threshold / }
} else {
catch {::legacy::set_attribute -quiet aupt_combo_input_count_threshold 150000 /}
}
}
set cur_attr_val [::legacy::get_attribute aupt__tolerance /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 10] [list $cur_attr_val]]} {
if {[is_attr_ifca aupt__tolerance ] && ![string compare 10 ""]} {
catch {::legacy::reset_attribute -quiet aupt__tolerance / }
} else {
catch {::legacy::set_attribute -quiet aupt__tolerance 10 /}
}
}
set cur_attr_val [::legacy::get_attribute pbs__ssyn_gs_part_enabled /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs__ssyn_gs_part_enabled ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet pbs__ssyn_gs_part_enabled / }
} else {
catch {::legacy::set_attribute -quiet pbs__ssyn_gs_part_enabled 0 /}
}
}
set cur_attr_val [::legacy::get_attribute pbs__ssyn_use_new_modes /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs__ssyn_use_new_modes ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet pbs__ssyn_use_new_modes / }
} else {
catch {::legacy::set_attribute -quiet pbs__ssyn_use_new_modes 0 /}
}
}
set cur_attr_val [::legacy::get_attribute pbs__pbs_stage /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list -1] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs__pbs_stage ] && ![string compare -1 ""]} {
catch {::legacy::reset_attribute -quiet pbs__pbs_stage / }
} else {
catch {::legacy::set_attribute -quiet pbs__pbs_stage -1 /}
}
}
set cur_attr_val [::legacy::get_attribute pbs__pbs_staged_flow /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list default] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs__pbs_staged_flow ] && ![string compare default ""]} {
catch {::legacy::reset_attribute -quiet pbs__pbs_staged_flow / }
} else {
catch {::legacy::set_attribute -quiet pbs__pbs_staged_flow default /}
}
}
set cur_attr_val [::legacy::get_attribute pbs_extreme_map /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs_extreme_map ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pbs_extreme_map / }
} else {
catch {::legacy::set_attribute -quiet pbs_extreme_map false /}
}
}
set cur_attr_val [::legacy::get_attribute write_design_strict_sdc /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca write_design_strict_sdc ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet write_design_strict_sdc / }
} else {
catch {::legacy::set_attribute -quiet write_design_strict_sdc false /}
}
}
set cur_attr_val [::legacy::get_attribute enc_preload_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca enc_preload_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet enc_preload_script / }
} else {
catch {::legacy::set_attribute -quiet enc_preload_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute invs_preload_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_preload_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet invs_preload_script / }
} else {
catch {::legacy::set_attribute -quiet invs_preload_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute enc_postload_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca enc_postload_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet enc_postload_script / }
} else {
catch {::legacy::set_attribute -quiet enc_postload_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute invs_postload_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_postload_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet invs_postload_script / }
} else {
catch {::legacy::set_attribute -quiet invs_postload_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute invs_post_plan_design_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_post_plan_design_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet invs_post_plan_design_script / }
} else {
catch {::legacy::set_attribute -quiet invs_post_plan_design_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute enc_preexport_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca enc_preexport_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet enc_preexport_script / }
} else {
catch {::legacy::set_attribute -quiet enc_preexport_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute invs_preexport_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_preexport_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet invs_preexport_script / }
} else {
catch {::legacy::set_attribute -quiet invs_preexport_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute enc_save_db /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca enc_save_db ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet enc_save_db / }
} else {
catch {::legacy::set_attribute -quiet enc_save_db false /}
}
}
set cur_attr_val [::legacy::get_attribute invs_save_db /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_save_db ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet invs_save_db / }
} else {
catch {::legacy::set_attribute -quiet invs_save_db false /}
}
}
set cur_attr_val [::legacy::get_attribute enc_enable_useful_skew /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca enc_enable_useful_skew ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet enc_enable_useful_skew / }
} else {
catch {::legacy::set_attribute -quiet enc_enable_useful_skew true /}
}
}
set cur_attr_val [::legacy::get_attribute invs_enable_useful_skew /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_enable_useful_skew ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet invs_enable_useful_skew / }
} else {
catch {::legacy::set_attribute -quiet invs_enable_useful_skew true /}
}
}
set cur_attr_val [::legacy::get_attribute invs_opt_leakage /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_opt_leakage ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet invs_opt_leakage / }
} else {
catch {::legacy::set_attribute -quiet invs_opt_leakage false /}
}
}
set cur_attr_val [::legacy::get_attribute invs_write_scandef_options /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_write_scandef_options ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet invs_write_scandef_options / }
} else {
catch {::legacy::set_attribute -quiet invs_write_scandef_options {} /}
}
}
set cur_attr_val [::legacy::get_attribute invs_opt_leakage_options /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_opt_leakage_options ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet invs_opt_leakage_options / }
} else {
catch {::legacy::set_attribute -quiet invs_opt_leakage_options {} /}
}
}
set cur_attr_val [::legacy::get_attribute ilm_hdls /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca ilm_hdls ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet ilm_hdls / }
} else {
catch {::legacy::set_attribute -quiet ilm_hdls {} /}
}
}
set cur_attr_val [::legacy::get_attribute c2c_hier_instances /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca c2c_hier_instances ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet c2c_hier_instances / }
} else {
catch {::legacy::set_attribute -quiet c2c_hier_instances {} /}
}
}
set cur_attr_val [::legacy::get_attribute multibit_consider_useful_skew /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca multibit_consider_useful_skew ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet multibit_consider_useful_skew / }
} else {
catch {::legacy::set_attribute -quiet multibit_consider_useful_skew false /}
}
}
set cur_attr_val [::legacy::get_attribute cpr__flag_cpr__whole_design /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca cpr__flag_cpr__whole_design ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet cpr__flag_cpr__whole_design / }
} else {
catch {::legacy::set_attribute -quiet cpr__flag_cpr__whole_design 0 /}
}
}
set cur_attr_val [::legacy::get_attribute spatial_delay_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list express] [list $cur_attr_val]]} {
if {[is_attr_ifca spatial_delay_effort ] && ![string compare express ""]} {
catch {::legacy::reset_attribute -quiet spatial_delay_effort / }
} else {
catch {::legacy::set_attribute -quiet spatial_delay_effort express /}
}
}
set cur_attr_val [::legacy::get_attribute spatial_gopt_cleanup_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list low] [list $cur_attr_val]]} {
if {[is_attr_ifca spatial_gopt_cleanup_effort ] && ![string compare low ""]} {
catch {::legacy::reset_attribute -quiet spatial_gopt_cleanup_effort / }
} else {
catch {::legacy::set_attribute -quiet spatial_gopt_cleanup_effort low /}
}
}
set cur_attr_val [::legacy::get_attribute phys_st_enable_cpr /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_st_enable_cpr ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet phys_st_enable_cpr / }
} else {
catch {::legacy::set_attribute -quiet phys_st_enable_cpr false /}
}
}
set cur_attr_val [::legacy::get_attribute lef_power_ground_pin_override /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca lef_power_ground_pin_override ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet lef_power_ground_pin_override / }
} else {
catch {::legacy::set_attribute -quiet lef_power_ground_pin_override 1 /}
}
}
set cur_attr_val [::legacy::get_attribute disable_power_ground_pin_support /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca disable_power_ground_pin_support ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet disable_power_ground_pin_support / }
} else {
catch {::legacy::set_attribute -quiet disable_power_ground_pin_support 0 /}
}
}
set cur_attr_val [::legacy::get_attribute plc_cpr_crit_nets /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 800] [list $cur_attr_val]]} {
if {[is_attr_ifca plc_cpr_crit_nets ] && ![string compare 800 ""]} {
catch {::legacy::reset_attribute -quiet plc_cpr_crit_nets / }
} else {
catch {::legacy::set_attribute -quiet plc_cpr_crit_nets 800 /}
}
}
set cur_attr_val [::legacy::get_attribute phys_aocv /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_aocv ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet phys_aocv / }
} else {
catch {::legacy::set_attribute -quiet phys_aocv false /}
}
}
set cur_attr_val [::legacy::get_attribute pam_cone_opt_aocv /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_cone_opt_aocv ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pam_cone_opt_aocv / }
} else {
catch {::legacy::set_attribute -quiet pam_cone_opt_aocv false /}
}
}
set cur_attr_val [::legacy::get_attribute pam_through_adjusts /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_through_adjusts ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pam_through_adjusts / }
} else {
catch {::legacy::set_attribute -quiet pam_through_adjusts false /}
}
}
set cur_attr_val [::legacy::get_attribute pam_positive_adjusts /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_positive_adjusts ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pam_positive_adjusts / }
} else {
catch {::legacy::set_attribute -quiet pam_positive_adjusts false /}
}
}
set cur_attr_val [::legacy::get_attribute pam_critical_cone_opt /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_critical_cone_opt ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet pam_critical_cone_opt / }
} else {
catch {::legacy::set_attribute -quiet pam_critical_cone_opt true /}
}
}
set cur_attr_val [::legacy::get_attribute pam_cone_opt_global_buffering /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_cone_opt_global_buffering ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pam_cone_opt_global_buffering / }
} else {
catch {::legacy::set_attribute -quiet pam_cone_opt_global_buffering false /}
}
}
set cur_attr_val [::legacy::get_attribute pam_cone_opt_skip_annotation /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pam_cone_opt_skip_annotation ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pam_cone_opt_skip_annotation / }
} else {
catch {::legacy::set_attribute -quiet pam_cone_opt_skip_annotation false /}
}
}
set cur_attr_val [::legacy::get_attribute report_num_violations_per_cg /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca report_num_violations_per_cg ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet report_num_violations_per_cg / }
} else {
catch {::legacy::set_attribute -quiet report_num_violations_per_cg false /}
}
}
set cur_attr_val [::legacy::get_attribute phys_update_preload_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_update_preload_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet phys_update_preload_script / }
} else {
catch {::legacy::set_attribute -quiet phys_update_preload_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute phys_update_preannotation_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_update_preannotation_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet phys_update_preannotation_script / }
} else {
catch {::legacy::set_attribute -quiet phys_update_preannotation_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute phys_post_piopt_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_post_piopt_script ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet phys_post_piopt_script / }
} else {
catch {::legacy::set_attribute -quiet phys_post_piopt_script no_value /}
}
}
set cur_attr_val [::legacy::get_attribute phys_annotate_ndr_nets /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_annotate_ndr_nets ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_annotate_ndr_nets / }
} else {
catch {::legacy::set_attribute -quiet phys_annotate_ndr_nets true /}
}
}
set cur_attr_val [::legacy::get_attribute edi_correlation_eco_mode /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca edi_correlation_eco_mode ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet edi_correlation_eco_mode / }
} else {
catch {::legacy::set_attribute -quiet edi_correlation_eco_mode false /}
}
}
set cur_attr_val [::legacy::get_attribute phys_keep_generic_flop_placement /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_keep_generic_flop_placement ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_keep_generic_flop_placement / }
} else {
catch {::legacy::set_attribute -quiet phys_keep_generic_flop_placement true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_use_read_script /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_use_read_script ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet phys_use_read_script / }
} else {
catch {::legacy::set_attribute -quiet phys_use_read_script false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_snapshot_dir /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list no_value] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_snapshot_dir ] && ![string compare no_value ""]} {
catch {::legacy::reset_attribute -quiet iopt_snapshot_dir / }
} else {
catch {::legacy::set_attribute -quiet iopt_snapshot_dir no_value /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_index_snapshot_dir /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_index_snapshot_dir ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet iopt_index_snapshot_dir / }
} else {
catch {::legacy::set_attribute -quiet iopt_index_snapshot_dir 0 /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_useful_skew_enable /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_useful_skew_enable ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_useful_skew_enable / }
} else {
catch {::legacy::set_attribute -quiet iopt_useful_skew_enable false /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_useful_skew_min_latency /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_useful_skew_min_latency ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet iopt_useful_skew_min_latency / }
} else {
catch {::legacy::set_attribute -quiet iopt_useful_skew_min_latency 0 /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_useful_skew_max_latency /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list inf] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_useful_skew_max_latency ] && ![string compare inf ""]} {
catch {::legacy::reset_attribute -quiet iopt_useful_skew_max_latency / }
} else {
catch {::legacy::set_attribute -quiet iopt_useful_skew_max_latency inf /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_useful_skew_max_skewpoints /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list inf] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_useful_skew_max_skewpoints ] && ![string compare inf ""]} {
catch {::legacy::reset_attribute -quiet iopt_useful_skew_max_skewpoints / }
} else {
catch {::legacy::set_attribute -quiet iopt_useful_skew_max_skewpoints inf /}
}
}
set cur_attr_val [::legacy::get_attribute iopt_useful_skew_macro_only /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca iopt_useful_skew_macro_only ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet iopt_useful_skew_macro_only / }
} else {
catch {::legacy::set_attribute -quiet iopt_useful_skew_macro_only false /}
}
}
set cur_attr_val [::legacy::get_attribute avoid_library_domain /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca avoid_library_domain ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet avoid_library_domain / }
} else {
catch {::legacy::set_attribute -quiet avoid_library_domain {} /}
}
}
set cur_attr_val [::legacy::get_attribute avoid_library_domain_count /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca avoid_library_domain_count ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet avoid_library_domain_count / }
} else {
catch {::legacy::set_attribute -quiet avoid_library_domain_count 0 /}
}
}
set cur_attr_val [::legacy::get_attribute invs_custom_cost_group_timing /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_custom_cost_group_timing ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet invs_custom_cost_group_timing / }
} else {
catch {::legacy::set_attribute -quiet invs_custom_cost_group_timing false /}
}
}
set cur_attr_val [::legacy::get_attribute cpr__flag_cpr__beginning_big_loop /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpr__flag_cpr__beginning_big_loop ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpr__flag_cpr__beginning_big_loop / }
} else {
catch {::legacy::set_attribute -quiet cpr__flag_cpr__beginning_big_loop 1 /}
}
}
set cur_attr_val [::legacy::get_attribute phys_enable_whatif_stack_via /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_enable_whatif_stack_via ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_enable_whatif_stack_via / }
} else {
catch {::legacy::set_attribute -quiet phys_enable_whatif_stack_via true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_svr_flow /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_svr_flow ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet phys_svr_flow / }
} else {
catch {::legacy::set_attribute -quiet phys_svr_flow true /}
}
}
set cur_attr_val [::legacy::get_attribute phys_svr_must_join /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_svr_must_join ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet phys_svr_must_join / }
} else {
catch {::legacy::set_attribute -quiet phys_svr_must_join false /}
}
}
set cur_attr_val [::legacy::get_attribute invs_stack_via_setup_filename /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca invs_stack_via_setup_filename ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet invs_stack_via_setup_filename / }
} else {
catch {::legacy::set_attribute -quiet invs_stack_via_setup_filename {} /}
}
}
set cur_attr_val [::legacy::get_attribute phys_stack_via_resistance /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_stack_via_resistance ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet phys_stack_via_resistance / }
} else {
catch {::legacy::set_attribute -quiet phys_stack_via_resistance {} /}
}
}
set cur_attr_val [::legacy::get_attribute phys_stack_via_layer_resistance /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca phys_stack_via_layer_resistance ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet phys_stack_via_layer_resistance / }
} else {
catch {::legacy::set_attribute -quiet phys_stack_via_layer_resistance {} /}
}
}
set cur_attr_val [::legacy::get_attribute ple_max_cap_per_unit_len /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1.0] [list $cur_attr_val]]} {
if {[is_attr_ifca ple_max_cap_per_unit_len ] && ![string compare 1.0 ""]} {
catch {::legacy::reset_attribute -quiet ple_max_cap_per_unit_len / }
} else {
catch {::legacy::set_attribute -quiet ple_max_cap_per_unit_len 1.0 /}
}
}
set cur_attr_val [::legacy::get_attribute ple_max_res_per_unit_len /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 250.0] [list $cur_attr_val]]} {
if {[is_attr_ifca ple_max_res_per_unit_len ] && ![string compare 250.0 ""]} {
catch {::legacy::reset_attribute -quiet ple_max_res_per_unit_len / }
} else {
catch {::legacy::set_attribute -quiet ple_max_res_per_unit_len 250.0 /}
}
}
set cur_attr_val [::legacy::get_attribute ple_max_area_per_unit_len /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 2.0] [list $cur_attr_val]]} {
if {[is_attr_ifca ple_max_area_per_unit_len ] && ![string compare 2.0 ""]} {
catch {::legacy::reset_attribute -quiet ple_max_area_per_unit_len / }
} else {
catch {::legacy::set_attribute -quiet ple_max_area_per_unit_len 2.0 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__debug /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__debug ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__debug / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__debug 0 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__preview /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__preview ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__preview / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__preview 0 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__timing_models_are_boundaries /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__timing_models_are_boundaries ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__timing_models_are_boundaries / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__timing_models_are_boundaries 1 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__preserve_as_user /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__preserve_as_user ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__preserve_as_user / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__preserve_as_user 1 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__ignore_buf_inv_tree_polarity /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__ignore_buf_inv_tree_polarity ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__ignore_buf_inv_tree_polarity / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__ignore_buf_inv_tree_polarity 1 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__do_collect_buffer_trees /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__do_collect_buffer_trees ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__do_collect_buffer_trees / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__do_collect_buffer_trees 1 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__local_loads_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 2] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__local_loads_threshold ] && ![string compare 2 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__local_loads_threshold / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__local_loads_threshold 2 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__leaf_loads_threshold /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 10] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__leaf_loads_threshold ] && ![string compare 10 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__leaf_loads_threshold / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__leaf_loads_threshold 10 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_synth__consider_constant_drivers /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_synth__consider_constant_drivers ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet cpf_synth__consider_constant_drivers / }
} else {
catch {::legacy::set_attribute -quiet cpf_synth__consider_constant_drivers 1 /}
}
}
set cur_attr_val [::legacy::get_attribute cpf_iso__honorDesignIsoRule /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 4] [list $cur_attr_val]]} {
if {[is_attr_ifca cpf_iso__honorDesignIsoRule ] && ![string compare 4 ""]} {
catch {::legacy::reset_attribute -quiet cpf_iso__honorDesignIsoRule / }
} else {
catch {::legacy::set_attribute -quiet cpf_iso__honorDesignIsoRule 4 /}
}
}
set cur_attr_val [::legacy::get_attribute flow_checks /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca flow_checks ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet flow_checks / }
} else {
catch {::legacy::set_attribute -quiet flow_checks {} /}
}
}
set cur_attr_val [::legacy::get_attribute new_check_flow /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca new_check_flow ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet new_check_flow / }
} else {
catch {::legacy::set_attribute -quiet new_check_flow false /}
}
}
set cur_attr_val [::legacy::get_attribute db_compatibility_for_non_seq_setup_arcs /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca db_compatibility_for_non_seq_setup_arcs ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet db_compatibility_for_non_seq_setup_arcs / }
} else {
catch {::legacy::set_attribute -quiet db_compatibility_for_non_seq_setup_arcs 0 /}
}
}
set cur_attr_val [::legacy::get_attribute derive_bussed_pins /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca derive_bussed_pins ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet derive_bussed_pins / }
} else {
catch {::legacy::set_attribute -quiet derive_bussed_pins false /}
}
}
set cur_attr_val [::legacy::get_attribute stats_db__suppress /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 0] [list $cur_attr_val]]} {
if {[is_attr_ifca stats_db__suppress ] && ![string compare 0 ""]} {
catch {::legacy::reset_attribute -quiet stats_db__suppress / }
} else {
catch {::legacy::set_attribute -quiet stats_db__suppress 0 /}
}
}
set cur_attr_val [::legacy::get_attribute tinfo_tstamp_file /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list .rs_grellert.tstamp] [list $cur_attr_val]]} {
if {[is_attr_ifca tinfo_tstamp_file ] && ![string compare .rs_grellert.tstamp ""]} {
catch {::legacy::reset_attribute -quiet tinfo_tstamp_file / }
} else {
catch {::legacy::set_attribute -quiet tinfo_tstamp_file .rs_grellert.tstamp /}
}
}
set cur_attr_val [::legacy::get_attribute metric_page_cfg /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca metric_page_cfg ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet metric_page_cfg / }
} else {
catch {::legacy::set_attribute -quiet metric_page_cfg {} /}
}
}
set cur_attr_val [::legacy::get_attribute startup_done /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca startup_done ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet startup_done / }
} else {
catch {::legacy::set_attribute -quiet startup_done true /}
}
}
set cur_attr_val [::legacy::get_attribute post_option /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca post_option ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet post_option / }
} else {
catch {::legacy::set_attribute -quiet post_option {} /}
}
}
set cur_attr_val [::legacy::get_attribute db_option /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca db_option ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet db_option / }
} else {
catch {::legacy::set_attribute -quiet db_option {} /}
}
}
set cur_attr_val [::legacy::get_attribute db_script_option /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca db_script_option ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet db_script_option / }
} else {
catch {::legacy::set_attribute -quiet db_script_option {} /}
}
}
set cur_attr_val [::legacy::get_attribute verbose_option /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca verbose_option ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet verbose_option / }
} else {
catch {::legacy::set_attribute -quiet verbose_option false /}
}
}
set cur_attr_val [::legacy::get_attribute report_tcl_command_error /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca report_tcl_command_error ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet report_tcl_command_error / }
} else {
catch {::legacy::set_attribute -quiet report_tcl_command_error false /}
}
}
set cur_attr_val [::legacy::get_attribute use_area_from_lef /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca use_area_from_lef ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet use_area_from_lef / }
} else {
catch {::legacy::set_attribute -quiet use_area_from_lef true /}
}
}
set cur_attr_val [::legacy::get_attribute script_search_path /]
if {(2 || [llength [split $cur_attr_val]]) && [string compare [list {. ../../}] [list $cur_attr_val]]} {
if {[is_attr_ifca script_search_path ] && ![string compare {. ../../} ""]} {
catch {::legacy::reset_attribute -quiet script_search_path / }
} else {
catch {::legacy::set_attribute -quiet script_search_path {. ../../} /}
}
}
set cur_attr_val [::legacy::get_attribute find_takes_multiple_names /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca find_takes_multiple_names ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet find_takes_multiple_names / }
} else {
catch {::legacy::set_attribute -quiet find_takes_multiple_names true /}
}
}
set cur_attr_val [::legacy::get_attribute hdl_track_filename_row_col /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca hdl_track_filename_row_col ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet hdl_track_filename_row_col / }
} else {
catch {::legacy::set_attribute -quiet hdl_track_filename_row_col true /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sim_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1000] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sim_effort ] && ![string compare 1000 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sim_effort / }
} else {
catch {::legacy::set_attribute -quiet simsat_sim_effort 1000 /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sim_max_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 8000] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sim_max_effort ] && ![string compare 8000 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sim_max_effort / }
} else {
catch {::legacy::set_attribute -quiet simsat_sim_max_effort 8000 /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sat_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 2200] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sat_effort ] && ![string compare 2200 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sat_effort / }
} else {
catch {::legacy::set_attribute -quiet simsat_sat_effort 2200 /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sat_max_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 6000] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sat_max_effort ] && ![string compare 6000 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sat_max_effort / }
} else {
catch {::legacy::set_attribute -quiet simsat_sat_max_effort 6000 /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sat_threshold_high_effort /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 2000] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sat_threshold_high_effort ] && ![string compare 2000 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sat_threshold_high_effort / }
} else {
catch {::legacy::set_attribute -quiet simsat_sat_threshold_high_effort 2000 /}
}
}
set cur_attr_val [::legacy::get_attribute simsat_sat_assign_limit /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 5000000] [list $cur_attr_val]]} {
if {[is_attr_ifca simsat_sat_assign_limit ] && ![string compare 5000000 ""]} {
catch {::legacy::reset_attribute -quiet simsat_sat_assign_limit / }
} else {
catch {::legacy::set_attribute -quiet simsat_sat_assign_limit 5000000 /}
}
}
set cur_attr_val [::legacy::get_attribute dont_set_region_leaf /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca dont_set_region_leaf ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet dont_set_region_leaf / }
} else {
catch {::legacy::set_attribute -quiet dont_set_region_leaf false /}
}
}
set cur_attr_val [::legacy::get_attribute max_cpus_per_server_int /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 8] [list $cur_attr_val]]} {
if {[is_attr_ifca max_cpus_per_server_int ] && ![string compare 8 ""]} {
catch {::legacy::reset_attribute -quiet max_cpus_per_server_int / }
} else {
catch {::legacy::set_attribute -quiet max_cpus_per_server_int 8 /}
}
}
set cur_attr_val [::legacy::get_attribute shm_id_rc /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 10584086] [list $cur_attr_val]]} {
if {[is_attr_ifca shm_id_rc ] && ![string compare 10584086 ""]} {
catch {::legacy::reset_attribute -quiet shm_id_rc / }
} else {
catch {::legacy::set_attribute -quiet shm_id_rc 10584086 /}
}
}
set cur_attr_val [::legacy::get_attribute continue_on_error /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca continue_on_error ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet continue_on_error / }
} else {
catch {::legacy::set_attribute -quiet continue_on_error false /}
}
}
set cur_attr_val [::legacy::get_attribute suppress_command_help /]
if {(0 || [llength [split $cur_attr_val]]) && [string compare [list {}] [list $cur_attr_val]]} {
if {[is_attr_ifca suppress_command_help ] && ![string compare {} ""]} {
catch {::legacy::reset_attribute -quiet suppress_command_help / }
} else {
catch {::legacy::set_attribute -quiet suppress_command_help {} /}
}
}
set cur_attr_val [::legacy::get_attribute excpt_clean_multiple_mc /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca excpt_clean_multiple_mc ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet excpt_clean_multiple_mc / }
} else {
catch {::legacy::set_attribute -quiet excpt_clean_multiple_mc false /}
}
}
set cur_attr_val [::legacy::get_attribute ui_respects_preserve /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca ui_respects_preserve ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet ui_respects_preserve / }
} else {
catch {::legacy::set_attribute -quiet ui_respects_preserve true /}
}
}
set cur_attr_val [::legacy::get_attribute sigint_happened /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca sigint_happened ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet sigint_happened / }
} else {
catch {::legacy::set_attribute -quiet sigint_happened false /}
}
}
set cur_attr_val [::legacy::get_attribute prune_iod_level /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 2] [list $cur_attr_val]]} {
if {[is_attr_ifca prune_iod_level ] && ![string compare 2 ""]} {
catch {::legacy::reset_attribute -quiet prune_iod_level / }
} else {
catch {::legacy::set_attribute -quiet prune_iod_level 2 /}
}
}
set cur_attr_val [::legacy::get_attribute map_mmmc_use_dominant_views /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca map_mmmc_use_dominant_views ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet map_mmmc_use_dominant_views / }
} else {
catch {::legacy::set_attribute -quiet map_mmmc_use_dominant_views false /}
}
}
set cur_attr_val [::legacy::get_attribute map_mmmc_use_sorted_views /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca map_mmmc_use_sorted_views ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet map_mmmc_use_sorted_views / }
} else {
catch {::legacy::set_attribute -quiet map_mmmc_use_sorted_views false /}
}
}
set cur_attr_val [::legacy::get_attribute force_ultra_global_mapping /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca force_ultra_global_mapping ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet force_ultra_global_mapping / }
} else {
catch {::legacy::set_attribute -quiet force_ultra_global_mapping true /}
}
}
set cur_attr_val [::legacy::get_attribute pbs_set_UGM_in_gen /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list true] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs_set_UGM_in_gen ] && ![string compare true ""]} {
catch {::legacy::reset_attribute -quiet pbs_set_UGM_in_gen / }
} else {
catch {::legacy::set_attribute -quiet pbs_set_UGM_in_gen true /}
}
}
set cur_attr_val [::legacy::get_attribute pbs_phys_partition_flow /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list false] [list $cur_attr_val]]} {
if {[is_attr_ifca pbs_phys_partition_flow ] && ![string compare false ""]} {
catch {::legacy::reset_attribute -quiet pbs_phys_partition_flow / }
} else {
catch {::legacy::set_attribute -quiet pbs_phys_partition_flow false /}
}
}
set cur_attr_val [::legacy::get_attribute gsp_gen_part_size /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 100000] [list $cur_attr_val]]} {
if {[is_attr_ifca gsp_gen_part_size ] && ![string compare 100000 ""]} {
catch {::legacy::reset_attribute -quiet gsp_gen_part_size / }
} else {
catch {::legacy::set_attribute -quiet gsp_gen_part_size 100000 /}
}
}
set cur_attr_val [::legacy::get_attribute dft_clear_cache_string_to_pin /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list 1] [list $cur_attr_val]]} {
if {[is_attr_ifca dft_clear_cache_string_to_pin ] && ![string compare 1 ""]} {
catch {::legacy::reset_attribute -quiet dft_clear_cache_string_to_pin / }
} else {
catch {::legacy::set_attribute -quiet dft_clear_cache_string_to_pin 1 /}
}
}
set cur_attr_val [::legacy::get_attribute st_master_tmp_directory /]
if {(1 || [llength [split $cur_attr_val]]) && [string compare [list /home/grellert/minicurso-sintese-ucpel/example/sbox_v1/rtl/.st_launch_twins2.inf.ufrgs.br_948] [list $cur_attr_val]]} {
if {[is_attr_ifca st_master_tmp_directory ] && ![string compare /home/grellert/minicurso-sintese-ucpel/example/sbox_v1/rtl/.st_launch_twins2.inf.ufrgs.br_948 ""]} {
catch {::legacy::reset_attribute -quiet st_master_tmp_directory / }
} else {
catch {::legacy::set_attribute -quiet st_master_tmp_directory /home/grellert/minicurso-sintese-ucpel/example/sbox_v1/rtl/.st_launch_twins2.inf.ufrgs.br_948 /}
}
}
if { "1.0"  ne "[::legacy::get_attr scale_of_cap_per_unit_length /]"} {
::legacy::set_attribute -quiet scale_of_cap_per_unit_length 1.0 /
}
if { "1.0"  ne "[::legacy::get_attr scale_of_res_per_unit_length /]"} {
::legacy::set_attribute -quiet scale_of_res_per_unit_length 1.0 /
}
if { "1.0"  ne "[::legacy::get_attr scale_of_area_per_unit_length /]"} {
::legacy::set_attribute -quiet scale_of_area_per_unit_length 1.0 /
}
if { "1.9000 2.0000"  ne "[::legacy::get_attr ple_correlation_factors /]"} {
::legacy::set_attribute -quiet ple_correlation_factors {1.9000 2.0000} /
}
if { "1.0"  ne "[::legacy::get_attr net_derating_factor_late_data /]"} {
::legacy::set_attribute -quiet net_derating_factor_late_data 1.0 /
}
if { "1.0"  ne "[::legacy::get_attr net_derating_factor_early_data /]"} {
::legacy::set_attribute -quiet net_derating_factor_early_data 1.0 /
}
if { "1.0"  ne "[::legacy::get_attr net_derating_factor_late_clock /]"} {
::legacy::set_attribute -quiet net_derating_factor_late_clock 1.0 /
}
if { "1.0"  ne "[::legacy::get_attr net_derating_factor_early_clock /]"} {
::legacy::set_attribute -quiet net_derating_factor_early_clock 1.0 /
}
if { "100"  ne "[::legacy::get_attr max_virtual_pins_for_each_port /]"} {
::legacy::set_attribute -quiet max_virtual_pins_for_each_port 100 /
}
if { "true"  ne "[::legacy::get_attr lib_lef_consistency_check_enable /]"} {
::legacy::set_attribute -quiet lib_lef_consistency_check_enable true /
}
if { "/pdk/st/cmos065_421/adv_EncounterTechnoKit_cmos065_7m4x0y2z_4.2/TECH/cmos065_7m4x0y2z_Worst.lef /pdk/st/cmos065_421/PRHS65_SNPS-AVT-CDS_5.0/CADENCE/LEF/PRHS65_soc.lef /pdk/st/cmos065_421/CORE65LPSVT_SNPS-AVT-CDS_4.1/CADENCE/LEF/CORE65LPSVT_soc.lef /pdk/st/cmos065_421/CLOCK65LPSVT_SNPS-AVT-CDS_2.1/CADENCE/LEF/CLOCK65LPSVT_soc.lef"  ne "[::legacy::get_attr lef_library /]"} {
::legacy::set_attribute -quiet lef_library {/pdk/st/cmos065_421/adv_EncounterTechnoKit_cmos065_7m4x0y2z_4.2/TECH/cmos065_7m4x0y2z_Worst.lef /pdk/st/cmos065_421/PRHS65_SNPS-AVT-CDS_5.0/CADENCE/LEF/PRHS65_soc.lef /pdk/st/cmos065_421/CORE65LPSVT_SNPS-AVT-CDS_4.1/CADENCE/LEF/CORE65LPSVT_soc.lef /pdk/st/cmos065_421/CLOCK65LPSVT_SNPS-AVT-CDS_2.1/CADENCE/LEF/CLOCK65LPSVT_soc.lef} /
}
if { "/pdk/st/cmos065_421/adv_EncounterTechnoKit_cmos065_7m4x0y2z_4.2/TECH/cmos065_7m4x0y2z_Best.captable"  ne "[::legacy::get_attr cap_table_file /]"} {
::legacy::set_attribute -quiet cap_table_file /pdk/st/cmos065_421/adv_EncounterTechnoKit_cmos065_7m4x0y2z_4.2/TECH/cmos065_7m4x0y2z_Best.captable /
}
if {[::legacy::get_attr pbs_eng_enable /] eq "false"} {
::legacy::set_attribute -quiet write_db_skip_floorplan 1 /
}
::legacy::set_attribute -quiet interconnect_mode ple /
::legacy::set_attribute -quiet ple_mode global /
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX10/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX103/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX124/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX14/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX17/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX21/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX24/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX27/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX31/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX34/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX38/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX41/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX45/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX48/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX52/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX55/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX58/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX62/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX7/Q}
::legacy::set_attribute -quiet user_function latch_posedge_precontrol {/libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX82/Q}
::legacy::set_attribute -quiet dp_tmd_downsizing 1 /
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX45/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX10/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX27/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX82/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX48/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX103/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX31/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX14/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX7/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX52/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX124/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX34/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX17/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX55/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX21/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX38/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX58/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX41/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX24/Q}
catch {::legacy::set_attribute -quiet user_function latch_posedge_precontrol /libraries/CORE65LPSVT/libcells/HS65_LS_CNHLSX62/Q}
set lib_cell_sets [::legacy::find /libraries -lib_cell_set *]
if { [llength $lib_cell_sets] } { delete_object $lib_cell_sets }
::legacy::get_attribute active_operating_conditions /
aae_init
set lib_load_end [::legacy::get_attribute runtime /]
set ::rc_st::bg::runtime_offset [expr {$lib_load_end - $lib_load_start}]
::rc_st::__eval_from_socket_terminator
